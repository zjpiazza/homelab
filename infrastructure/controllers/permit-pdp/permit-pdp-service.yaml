apiVersion: v1
kind: Service
metadata:
  name: permit-pdp
  namespace: permit-pdp
spec:
  type: ClusterIP
  ports:
    - port: 7000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: permit-pdp
