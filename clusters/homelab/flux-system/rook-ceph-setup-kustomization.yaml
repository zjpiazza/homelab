apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: rook-ceph-setup
  namespace: flux-system
spec:
  interval: 10m0s
  dependsOn:
    - name: flux-system
  path: ./clusters/homelab/rook-ceph-setup
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
